name: build

on:
  workflow_run:
    workflows:
      - "unit tests"
    branches:
      - main
    types:
      - completed

jobs:
  build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read
    uses: pysmo/.github/.github/workflows/build.yml@main
